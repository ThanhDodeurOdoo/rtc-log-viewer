<templates>
    <t t-name="UploadPanel">
        <div class="file-upload-container" t-attf-class="file-upload-container {{ this.isDragOver() ? 'drag-over' : '' }}" t-on-dragover.prevent.stop="this.onDragOver" t-on-dragleave.prevent.stop="this.onDragLeave" t-on-drop.prevent.stop="this.onFileDrop">
            <h2>RTC Log Viewer</h2>
            <p>Upload a JSON log file (<a target="_blank" href="https://www.odoo.com/knowledge/article/28833">from Odoo Discuss RTC</a>)</p>
            <p class="download-info">logs are analyzed locally and stay on your device</p>
            <div class="drop-zone">
                <div class="drop-zone-prompt">
                    <i class="drop-icon" />
                    <p>Drag and drop your JSON log file here</p>
                    <p>or</p>
                </div>
                <div class="file-input">
                    <input type="file" accept=".json" t-ref="this.fileInputRef" t-on-change="this.onFileChange" />
                    <button t-on-click="this.triggerFileInput">Choose File</button>
                    <span t-if="this.fileName()" class="file-name" t-out="this.fileName()" />
                    <span t-else="" class="file-hint">No file chosen</span>
                </div>
                <div class="demo-loader">
                    <button class="demo-button" t-on-click="this.loadDemoData">Load demo data</button>
                </div>
            </div>
        </div>
    </t>
</templates>
